import sampleRoute from './sample';

/**
 * ### Routes constructor
 * - Put all imported routes in the routes constructor array.
 * - The routes constructor is being used  as middleware in the `startApp()` function.
 */
const routes = [sampleRoute];

export default routes;
